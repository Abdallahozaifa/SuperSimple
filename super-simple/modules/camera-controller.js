/* global createjs, $, jQuery, superSimple, player */

superSimple.camera = {
    _zoomFactor: 1,
    // Returns zoom factor
    getAdjustedSize: function (size) {
        return this._zoomFactor*size;
    },
    // Updates zoom factor
    update: function () {
        
    },
    // runs every tick
    tick: function () {
        
    }
};